import { actions } from "./protocols.actions";
// import { queries } from "./protocols.queries";
import { selectors } from "./protocols.selectors";

export const protocols = {
  actions: actions,
  selectors: selectors,
  // queries: queries
};
