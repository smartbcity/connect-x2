
const dashboard = "/";
const sessions = "/:protocol/sessions";
const sessionDetails = "/:channelId/:chaincodeId/:ssmName/:sessionName/details";

export const path = {
  dashboard: dashboard,
  sessions: sessions,
  sessionDetails: sessionDetails,
};