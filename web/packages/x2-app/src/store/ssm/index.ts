import { actions } from "./ssm.actions";
import { queries } from "./ssm.queries";
import { selectors } from "./ssm.selectors";

export const ssm = {
  actions: actions,
  selectors: selectors,
  queries: queries
};
